<UserControl x:Class="Wemail.schedule.Views.ScheduleView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:prism="http://prismlibrary.com/"             
             prism:ViewModelLocator.AutoWireViewModel="True">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="2*"/>
            <ColumnDefinition Width="8*"/>
        </Grid.ColumnDefinitions>
        <DatePicker x:Name="MyDatePicker" ></DatePicker>
        <ContentControl Grid.Column="1" Content="{Binding ElementName=MyDatePicker,Path=Text}" HorizontalAlignment="Center" VerticalAlignment="Center" FontSize="40"></ContentControl>
    </Grid>
</UserControl>
